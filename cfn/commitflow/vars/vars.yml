---
GITHUB_PAT: "{{ VAULT_GITHUB_PAT }}"
GITLAB_PAT: "{{ VAULT_GITLAB_PAT }}"
POSTGRES_USER: postgres
POSTGRES_PASSWORD: "{{ VAULT_POSTGRES_PASSWORD }}"
GRAFANA_USER: grafana
GRAFANA_PASSWORD: "{{ VAULT_GRAFANA_PASSWORD }}"
